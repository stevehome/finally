---
phase: 07-testing
plan: 3
type: execute
wave: 2
depends_on: [07-01-PLAN.md, 07-02-PLAN.md]
files_modified:
  - test/package.json
  - test/playwright.config.ts
  - docker-compose.test.yml
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "test/package.json exists with playwright as a dependency and a test script"
    - "test/playwright.config.ts exists with baseURL, timeout, chromium project"
    - "docker-compose.test.yml exists at project root"
    - "`docker compose -f docker-compose.test.yml config` validates without errors"
    - "app service uses build: context (not image: finally) so no pre-built image required"
    - "playwright service depends_on app with condition: service_healthy"
    - "both services share a named network so playwright can reach http://app:8000"
  artifacts:
    - path: "test/package.json"
      provides: "npm project definition with playwright dep and test script"
    - path: "test/playwright.config.ts"
      provides: "playwright config: baseURL from env, chromium project, 30s timeout, 1 retry"
    - path: "docker-compose.test.yml"
      provides: "compose file that builds app + runs playwright tests in containers"
---
<objective>
Create the E2E infrastructure: test/package.json, test/playwright.config.ts, and docker-compose.test.yml at the project root. The compose file must validate with `docker compose config`. The playwright config must set up chromium with baseURL from BASE_URL env var.
</objective>

<execution_context>
@/Users/steve/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steve/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steve/projects/finally/Dockerfile
@/Users/steve/projects/finally/.planning/phases/07-testing/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Determine installed playwright version</name>
  <files>test/node_modules/playwright/</files>
  <action>
Read the playwright version from the installed package to ensure the Docker image tag matches exactly. Check test/node_modules/playwright/lib/common/configLoader.js or test/node_modules/playwright-core/lib/client/api.js for a version string. If the version cannot be determined from files, use the presence of MCP agent files (lib/agents/, lib/mcp/) as an indicator that this is Playwright >= 1.52 and use `v1.52.0-jammy` as the Docker image tag.

To check: look for any file that exports or references a version number in the installed playwright package.
  </action>
  <verify>ls /Users/steve/projects/finally/test/node_modules/playwright/lib/agents/ 2>/dev/null && echo "has agents (>=1.52)" || echo "no agents dir"</verify>
  <done>Version is known. If agents directory exists, use v1.52.0-jammy. Otherwise use v1.51.1-jammy.</done>
</task>

<task type="auto">
  <name>Task 2: Create test/package.json</name>
  <files>test/package.json</files>
  <action>
Create /Users/steve/projects/finally/test/package.json with the following content. Use the playwright version determined in Task 1 (1.52.0 if agents dir exists, 1.51.1 otherwise).

```json
{
  "name": "finally-e2e",
  "version": "1.0.0",
  "description": "E2E tests for FinAlly using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "typescript": "^5.0.0"
  }
}
```

Note: The node_modules directory already contains playwright and playwright-core binaries installed separately. The package.json is needed for `npx playwright test` to find the config and resolve the test runner. The existing node_modules will be used as-is inside the Docker container (mounted volume).
  </action>
  <verify>cat /Users/steve/projects/finally/test/package.json</verify>
  <done>package.json exists and contains "playwright" and "test" script</done>
</task>

<task type="auto">
  <name>Task 3: Create test/playwright.config.ts</name>
  <files>test/playwright.config.ts</files>
  <action>
Create /Users/steve/projects/finally/test/playwright.config.ts with the following content:

```typescript
import { defineConfig, devices } from '@playwright/test';

const BASE_URL = process.env.BASE_URL || 'http://localhost:8000';

export default defineConfig({
  testDir: './e2e',
  timeout: 30_000,
  retries: 1,
  reporter: [['list']],
  use: {
    baseURL: BASE_URL,
    headless: true,
    screenshot: 'only-on-failure',
    trace: 'retain-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
});
```
  </action>
  <verify>cat /Users/steve/projects/finally/test/playwright.config.ts</verify>
  <done>File exists and contains defineConfig, baseURL, chromium project, 30_000 timeout</done>
</task>

<task type="auto">
  <name>Task 4: Create test/e2e/ directory placeholder</name>
  <files>test/e2e/.gitkeep</files>
  <action>
Create the e2e subdirectory that playwright.config.ts points to as testDir. Create an empty placeholder file so the directory is tracked.

Write an empty file at /Users/steve/projects/finally/test/e2e/.gitkeep
  </action>
  <verify>ls /Users/steve/projects/finally/test/e2e/</verify>
  <done>test/e2e/ directory exists</done>
</task>

<task type="auto">
  <name>Task 5: Create docker-compose.test.yml at project root</name>
  <files>docker-compose.test.yml</files>
  <action>
Create /Users/steve/projects/finally/docker-compose.test.yml with the following content.

Key decisions:
- Use `build: .` so the compose file builds the `finally` image from the Dockerfile in the project root — no pre-built image required
- Use a named network `testnet` so the playwright container can reach the app at `http://app:8000`
- Override healthcheck to use a tighter interval (5s) than the Dockerfile default (30s)
- Mount `./test` as `/tests` in the playwright container
- Pass `BASE_URL=http://app:8000` so playwright.config.ts uses the service hostname

```yaml
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - LLM_MOCK=true
    volumes:
      - test-db:/app/db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - testnet

  playwright:
    image: mcr.microsoft.com/playwright:v1.52.0-jammy
    working_dir: /tests
    volumes:
      - ./test:/tests
    depends_on:
      app:
        condition: service_healthy
    environment:
      - BASE_URL=http://app:8000
    command: npx playwright test --reporter=list
    networks:
      - testnet

volumes:
  test-db:

networks:
  testnet:
    driver: bridge
```
  </action>
  <verify>docker compose -f /Users/steve/projects/finally/docker-compose.test.yml config 2>&1 | head -20</verify>
  <done>`docker compose config` outputs YAML without errors (no "error" or "invalid" lines at start)</done>
</task>

</tasks>

<verification>
1. `cat /Users/steve/projects/finally/test/package.json` — shows playwright devDependency and test script
2. `cat /Users/steve/projects/finally/test/playwright.config.ts` — shows defineConfig with e2e testDir, chromium, 30s timeout
3. `docker compose -f /Users/steve/projects/finally/docker-compose.test.yml config` — outputs valid YAML, no errors
4. `ls /Users/steve/projects/finally/test/e2e/` — directory exists
</verification>

<success_criteria>
- test/package.json created with playwright devDependency
- test/playwright.config.ts created pointing to ./e2e with chromium project and BASE_URL from env
- docker-compose.test.yml at project root passes `docker compose config` validation
- app service uses build: context (not image:) — no pre-built image required
- playwright service depends on app with service_healthy condition
- both services share testnet network
</success_criteria>

<output>After completion, create /Users/steve/projects/finally/.planning/phases/07-testing/07-03-SUMMARY.md</output>
