---
phase: 04-frontend-shell-watchlist
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - frontend/components/Header.tsx
  - frontend/components/AppShell.tsx
autonomous: false
requirements:
  - FE-03
  - FE-04

must_haves:
  truths:
    - "Header shows portfolio total value updating live as prices change"
    - "Header shows cash balance"
    - "Connection status dot is green when SSE is connected, red when disconnected"
    - "Disconnecting from SSE (stop backend) turns dot red; reconnecting turns it green again"
    - "Full phase success criteria met: dark theme, header, watchlist, sparklines, flash, selection"
  artifacts:
    - path: "frontend/components/Header.tsx"
      provides: "Standalone header component showing total value, cash, connection dot"
      exports: ["Header"]
  key_links:
    - from: "frontend/components/Header.tsx"
      to: "usePortfolio hook"
      via: "portfolio.total_value, portfolio.cash_balance props"
    - from: "frontend/components/Header.tsx"
      to: "usePriceStream connected state"
      via: "connected prop drives dot color"
---

<objective>
Extract the header into a standalone Header component with proper connection status dot behavior, wire it cleanly in AppShell, and do a final end-to-end verification of all Phase 4 success criteria.

Purpose: Completes the header as a self-contained component (Phase 5 will add more header elements), confirms FE-03 and FE-04 are fully met, and signs off the phase.
Output: Phase 4 complete — all 5 success criteria verified.
</objective>

<execution_context>
@/Users/steve/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steve/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-shell-watchlist/RESEARCH.md
@.planning/phases/04-frontend-shell-watchlist/04-frontend-03-SUMMARY.md
@frontend/components/AppShell.tsx
@frontend/hooks/usePortfolio.ts
@frontend/hooks/usePriceStream.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Header component</name>
  <files>frontend/components/Header.tsx, frontend/components/AppShell.tsx</files>
  <action>
    Create `frontend/components/Header.tsx`:

    ```tsx
    'use client'

    import type { Portfolio } from '@/types/market'

    interface HeaderProps {
      portfolio: Portfolio | null
      connected: boolean
    }

    function fmt(value: number): string {
      return value.toLocaleString('en-US', {
        minimumFractionDigits: 2,
        maximumFractionDigits: 2,
      })
    }

    export default function Header({ portfolio, connected }: HeaderProps) {
      return (
        <div
          className="col-span-2 flex items-center justify-between px-4 py-2 border-b"
          style={{ borderColor: '#30363d', backgroundColor: '#161b22' }}
        >
          {/* Logo */}
          <div className="flex items-center gap-3">
            <span className="text-accent font-bold tracking-widest text-sm font-mono">
              FINALLY
            </span>
            <span className="text-text-muted text-xs hidden sm:block">
              AI Trading Workstation
            </span>
          </div>

          {/* Portfolio values + connection status */}
          <div className="flex items-center gap-6 text-sm">
            <div className="flex flex-col items-end">
              <span className="text-text-muted text-xs leading-none">Portfolio</span>
              <span className="text-text-primary font-mono font-semibold">
                {portfolio ? `$${fmt(portfolio.total_value)}` : '—'}
              </span>
            </div>

            <div className="flex flex-col items-end">
              <span className="text-text-muted text-xs leading-none">Cash</span>
              <span className="text-text-primary font-mono">
                {portfolio ? `$${fmt(portfolio.cash_balance)}` : '—'}
              </span>
            </div>

            {/* Connection status dot with label */}
            <div className="flex items-center gap-1.5">
              <span
                className="inline-block w-2 h-2 rounded-full transition-colors duration-300"
                style={{ backgroundColor: connected ? '#22c55e' : '#ef4444' }}
                title={connected ? 'Connected to live stream' : 'Disconnected — reconnecting...'}
              />
              <span
                className="text-xs font-mono"
                style={{ color: connected ? '#22c55e' : '#ef4444' }}
              >
                {connected ? 'LIVE' : 'DISC'}
              </span>
            </div>
          </div>
        </div>
      )
    }
    ```

    Update `frontend/components/AppShell.tsx` to use the Header component.
    Replace the inline header JSX with `<Header portfolio={portfolio} connected={connected} />`.
    Add the import: `import Header from './Header'`.

    The complete updated AppShell (minimal diff — only the header section changes):

    ```tsx
    'use client'

    import { useState } from 'react'
    import { usePriceStream } from '@/hooks/usePriceStream'
    import { usePortfolio } from '@/hooks/usePortfolio'
    import { useWatchlist } from '@/hooks/useWatchlist'
    import Header from './Header'
    import WatchlistPanel from './WatchlistPanel'

    export default function AppShell() {
      const { prices, connected, sparkHistory } = usePriceStream()
      const portfolio = usePortfolio()
      const tickers = useWatchlist()
      const [selectedTicker, setSelectedTicker] = useState<string | null>(null)

      return (
        <div className="grid h-screen" style={{
          gridTemplateRows: 'auto 1fr',
          gridTemplateColumns: '300px 1fr',
        }}>
          <Header portfolio={portfolio} connected={connected} />

          {/* Watchlist column */}
          <div
            className="overflow-y-auto border-r"
            style={{ borderColor: '#30363d', backgroundColor: '#0d1117' }}
          >
            <div
              className="text-xs text-text-muted uppercase tracking-wider px-3 py-2 border-b"
              style={{ borderColor: '#30363d' }}
            >
              Watchlist
            </div>
            <WatchlistPanel
              tickers={tickers}
              prices={prices}
              sparkHistory={sparkHistory}
              selectedTicker={selectedTicker}
              onSelectTicker={setSelectedTicker}
            />
          </div>

          {/* Main area — placeholder for Phase 5 */}
          <div
            className="flex items-center justify-center"
            style={{ backgroundColor: '#0d1117' }}
          >
            {selectedTicker ? (
              <p className="text-text-muted text-sm font-mono">
                {selectedTicker} — chart coming in Phase 5
              </p>
            ) : (
              <p className="text-text-muted text-xs">Select a ticker to view chart</p>
            )}
          </div>
        </div>
      )
    }
    ```

    Note: `portfolio.total_value` from usePortfolio polls every 5 seconds. This is sufficient for
    the header display — it doesn't need SSE-level update frequency. Phase 5 will add a more reactive
    portfolio display (the heatmap and positions table).
  </action>
  <verify>
    ```bash
    cd /Users/steve/projects/finally/frontend
    npm run build 2>&1 | tail -20
    ```
    Must compile cleanly.
  </verify>
  <done>
    Header.tsx is a clean standalone component. AppShell uses it. Build passes.
    Header shows "FINALLY" logo, portfolio value, cash, and connection dot.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 4 frontend: Next.js dark terminal served by FastAPI, SSE live prices, watchlist
    with flash animations + sparklines + ticker selection, header with portfolio values and connection
    status dot.
  </what-built>
  <how-to-verify>
    1. Build the frontend:
       ```bash
       cd /Users/steve/projects/finally/frontend && npm run build
       ```

    2. Start the backend:
       ```bash
       cd /Users/steve/projects/finally/backend && uv run uvicorn main:app --port 8000
       ```

    3. Open http://localhost:8000 in a browser and verify ALL of the following:

       **SC-1 — Dark terminal aesthetic:**
       - Background is dark (~#0d1117), not white
       - Header bar is slightly lighter (#161b22)
       - "FINALLY" logo text is gold/yellow (#ecad0a)

       **SC-2 — Header:**
       - Portfolio total value shows a dollar amount (e.g., "$10,000.00") — not "—"
       - Cash balance shows "$10,000.00" or lower if trades executed
       - Connection dot is GREEN (not red) and shows "LIVE"

       **SC-3 — Watchlist:**
       - Left column shows all 10 tickers: AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX
       - Each row shows price (e.g., "$190.52") and change % (e.g., "+0.016%")
       - Sparklines appear as small line charts beside each row (may take 10-15s to accumulate enough points)

       **SC-4 — Price flash animations:**
       - Watch the watchlist for 10-15 seconds
       - Rows should flash green or red briefly as prices update
       - Flash fades out in ~500ms

       **SC-5 — Ticker selection:**
       - Click any ticker in the watchlist
       - That row gets a gold left border (#ecad0a) and slightly darker background
       - Main area shows "{TICKER} — chart coming in Phase 5"
       - Clicking a different ticker deselects the first, selects the new one

       **SC-6 — Reconnection (optional but impressive):**
       - Stop the backend (Ctrl+C)
       - Connection dot turns RED and shows "DISC" within ~2-3 seconds
       - Restart the backend
       - Dot turns GREEN again and prices resume updating

  </how-to-verify>
  <resume-signal>
    Type "approved" if all criteria pass. If issues found, describe which SC failed and what was observed.
  </resume-signal>
</task>

</tasks>

<verification>
The checkpoint task IS the verification for this plan. All 5 Phase 4 success criteria are checked
interactively in the human-verify step above.

Automated gate before the checkpoint:
```bash
cd /Users/steve/projects/finally/frontend && npm run build
```
Must exit 0.
</verification>

<success_criteria>
- FE-01: Next.js app builds and FastAPI serves it at root (verified by curl + browser)
- FE-02: Dark terminal aesthetic (#0d1117, accent #ecad0a, blue #209dd7) visible
- FE-03: Header shows live portfolio total value, cash balance, connection dot
- FE-04: SSE connects on page load (green dot), reconnects automatically (dot cycles red→green)
- WUI-01: All 10 watchlist tickers displayed
- WUI-02: Symbol, price, change %, sparkline per row
- WUI-03: Price flash green/red with ~500ms fade
- WUI-04: Sparklines accumulate over time from SSE stream
- WUI-05: Clicking a ticker selects it with visible highlight
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-shell-watchlist/04-frontend-04-SUMMARY.md`
</output>
