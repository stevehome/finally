---
phase: 06-docker-deployment
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package-lock.json
  - db/.gitkeep
  - .dockerignore
autonomous: true
requirements:
  - DOCK-01
  - DOCK-03
must_haves:
  truths:
    - "frontend/package-lock.json exists and is valid (generated by npm install)"
    - "db/.gitkeep exists so the db/ directory is tracked by git"
    - ".dockerignore exists at project root"
  artifacts:
    - path: "frontend/package-lock.json"
      provides: "Lockfile enabling npm ci in Docker Stage 1"
    - path: "db/.gitkeep"
      provides: "Ensures db/ directory exists in repo for volume mount target"
    - path: ".dockerignore"
      provides: "Excludes node_modules, .venv, .env, db/*.db, .git from Docker build context"
---

<objective>
Generate the three prerequisite files required before the Dockerfile can be built:
1. `frontend/package-lock.json` — generated by `npm install` in the frontend directory; required for `npm ci` in Docker.
2. `db/.gitkeep` — creates the `db/` directory in the repo so the Docker volume mount target exists.
3. `.dockerignore` — tells Docker which files to exclude from the build context.

No Docker commands are run in this wave.
</objective>

<execution_context>
@/Users/steve/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steve/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/steve/projects/finally/frontend/package.json
@/Users/steve/projects/finally/frontend/next.config.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Generate frontend/package-lock.json</name>
  <files>frontend/package-lock.json</files>
  <action>
Run `npm install` inside the frontend directory to generate package-lock.json.

```bash
cd /Users/steve/projects/finally/frontend && npm install
```

This creates `frontend/package-lock.json` with exact version pins for all dependencies listed in `package.json`. Do not edit `package.json` — just run npm install and let it generate the lockfile.
  </action>
  <verify>ls -lh /Users/steve/projects/finally/frontend/package-lock.json</verify>
  <done>File exists and is non-empty (typically several hundred KB).</done>
</task>

<task type="auto">
  <name>Task 2: Create db/.gitkeep</name>
  <files>db/.gitkeep</files>
  <action>
Create an empty `.gitkeep` file at `db/.gitkeep` so the `db/` directory is tracked by git. This directory is where the Docker volume `finally-data` will be mounted at `/app/db` inside the container.

Create the file with empty contents at `/Users/steve/projects/finally/db/.gitkeep`.
  </action>
  <verify>ls -la /Users/steve/projects/finally/db/</verify>
  <done>db/.gitkeep exists (zero bytes is correct).</done>
</task>

<task type="auto">
  <name>Task 3: Create .dockerignore</name>
  <files>.dockerignore</files>
  <action>
Create `.dockerignore` at `/Users/steve/projects/finally/.dockerignore` with the following exact contents:

```
# Node
frontend/node_modules/
frontend/.next/
frontend/.next/cache/

# Python
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/**/*.pyc

# Database (lives on a volume, not baked into image)
db/*.db

# Secrets and env
.env

# Planning docs (not needed in image)
.planning/
planning/

# Git
.git/
.gitignore

# Scripts (not needed inside container)
scripts/

# Test artifacts
test/
```
  </action>
  <verify>cat /Users/steve/projects/finally/.dockerignore</verify>
  <done>File exists and contains the sections above.</done>
</task>
</tasks>

<verification>
Run these commands to confirm all three artifacts are in place:

```bash
ls -lh /Users/steve/projects/finally/frontend/package-lock.json
ls -la /Users/steve/projects/finally/db/.gitkeep
cat /Users/steve/projects/finally/.dockerignore
```

Expected:
- `package-lock.json` exists and is non-empty
- `db/.gitkeep` exists (zero bytes is fine)
- `.dockerignore` contains the node_modules, .venv, .env, and db/*.db exclusions
</verification>

<success_criteria>
- `frontend/package-lock.json` exists and npm can use it (`npm ci --dry-run` in frontend/ exits 0)
- `db/.gitkeep` exists
- `.dockerignore` exists at project root and contains `frontend/node_modules/`, `backend/.venv/`, `.env`, and `db/*.db`
</success_criteria>

<output>After completion, create /Users/steve/projects/finally/.planning/phases/06-docker-deployment/06-01-SUMMARY.md</output>
