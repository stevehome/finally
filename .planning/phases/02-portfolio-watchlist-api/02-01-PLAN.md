---
phase: 02-portfolio-watchlist-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_portfolio.py
  - backend/tests/test_watchlist.py
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - PORT-06
  - PORT-07
  - PORT-08
  - WATCH-01
  - WATCH-02
  - WATCH-03
  - WATCH-04
  - WATCH-05

must_haves:
  truths:
    - "All test stubs exist and are collected by pytest"
    - "All stubs fail with xfail or NotImplementedError, not import errors"
    - "Running the full test suite does not error on import"
  artifacts:
    - path: "backend/tests/test_portfolio.py"
      provides: "PORT-01 through PORT-08 test stubs"
      exports: ["test_get_portfolio", "test_buy_trade", "test_sell_trade", "test_buy_insufficient_cash",
                "test_sell_insufficient_shares", "test_trade_history_recorded", "test_snapshot_after_trade",
                "test_snapshot_background_task", "test_portfolio_history"]
    - path: "backend/tests/test_watchlist.py"
      provides: "WATCH-01 through WATCH-05 test stubs"
      exports: ["test_get_watchlist", "test_add_ticker", "test_remove_ticker",
                "test_add_ticker_starts_streaming", "test_remove_ticker_stops_streaming"]
  key_links:
    - from: "backend/tests/test_portfolio.py"
      to: "backend/tests/conftest.py"
      via: "tmp_db autouse fixture"
      pattern: "tmp_db"
    - from: "backend/tests/test_watchlist.py"
      to: "backend/tests/conftest.py"
      via: "tmp_db autouse fixture"
      pattern: "tmp_db"
---

<objective>
Create failing test stubs for all Phase 2 requirements so that RED-phase tests exist before any implementation begins.

Purpose: Nyquist compliance — every PORT and WATCH requirement has a named test that fails before code is written. Implementation plans in Wave 2+ turn these red tests green.
Output: Two test files with all required test functions, each marked xfail with a descriptive reason. No import of router modules that don't exist yet.
</objective>

<execution_context>
@/Users/steve/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steve/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-watchlist-api/02-VALIDATION.md

@backend/tests/conftest.py
@backend/tests/test_main.py
@backend/app/db.py
</context>

<interfaces>
<!-- Key contracts the executor needs. No router modules exist yet — stubs must not import them. -->

From backend/tests/conftest.py:
```python
@pytest.fixture(autouse=True)
def tmp_db(tmp_path, monkeypatch):
    """Redirect DB_PATH to a temp file so tests never touch db/finally.db."""
    # Sets env var DB_PATH and monkeypatches app.db.DB_PATH
    # autouse=True — every test gets a fresh isolated DB automatically
    yield db_file  # str path to the temp SQLite file
```

From backend/app/db.py:
```python
def init_db() -> None: ...   # Creates schema + seeds default user + 10 watchlist tickers
def get_connection() -> sqlite3.Connection: ...  # row_factory = sqlite3.Row
def get_watchlist_tickers(user_id: str = "default") -> list[str]: ...

# Schema tables: users_profile, watchlist, positions, trades, portfolio_snapshots, chat_messages
# Default seed: user "default" with cash_balance=10000.0, watchlist of 10 tickers
```

From backend/main.py:
```python
from fastapi.testclient import TestClient
from main import app
# TestClient(app) starts lifespan (init_db + market data source)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Portfolio test stubs (PORT-01 through PORT-08)</name>
  <files>backend/tests/test_portfolio.py</files>
  <behavior>
    - test_get_portfolio: xfail — GET /api/portfolio returns 200 with cash_balance, positions list, total_value
    - test_buy_trade: xfail — POST /api/portfolio/trade {ticker, quantity, side:"buy"} returns 200; cash decreases; position created
    - test_sell_trade: xfail — POST /api/portfolio/trade {side:"sell"} returns 200; cash increases; position quantity decreases
    - test_buy_insufficient_cash: xfail — buy when cash < cost returns 400 (not 500)
    - test_sell_insufficient_shares: xfail — sell more than owned returns 400 (not 500)
    - test_trade_history_recorded: xfail — after trade, trades table has one row for that ticker
    - test_snapshot_after_trade: xfail — after trade, portfolio_snapshots table has one row
    - test_snapshot_background_task: xfail — snapshot loop function records a snapshot row when called
    - test_portfolio_history: xfail — GET /api/portfolio/history returns 200 with a list of snapshots
  </behavior>
  <action>
Create `backend/tests/test_portfolio.py`. Do not import `app.routers.portfolio` (it doesn't exist yet).

Use `pytest.mark.xfail(reason="...", strict=True)` on every test function to mark them as expected failures.

Each test should:
1. Import only `pytest`, `sqlite3`, standard library, `main.app`, and `fastapi.testclient.TestClient`
2. Use `TestClient(app)` as a context manager to trigger lifespan
3. Make the HTTP call that the requirement demands
4. Assert the response code and minimal response shape

For test_snapshot_background_task: import the not-yet-existing function path with a try/except ImportError inside the test body, then call `pytest.fail("not implemented")` — this way it fails cleanly without a collection error.

Mark every test with both `@pytest.mark.xfail(reason="PORT-XX: router not implemented", strict=True)`.

File header must include module docstring and imports. Follow project style (100-char lines, snake_case, Google-style docstrings).
  </action>
  <verify>
    <automated>cd /Users/steve/projects/finally/backend && uv run --extra dev pytest tests/test_portfolio.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>pytest collects 9 tests from test_portfolio.py; all show XFAIL; no ERRORS in collection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Watchlist test stubs (WATCH-01 through WATCH-05)</name>
  <files>backend/tests/test_watchlist.py</files>
  <behavior>
    - test_get_watchlist: xfail — GET /api/watchlist returns 200 with a list of objects each containing ticker and price
    - test_add_ticker: xfail — POST /api/watchlist {ticker:"PYPL"} returns 201; ticker appears in subsequent GET
    - test_remove_ticker: xfail — DELETE /api/watchlist/AAPL returns 200 or 204; ticker gone from GET
    - test_add_ticker_starts_streaming: xfail — POST /api/watchlist calls source.add_ticker (verified via mock)
    - test_remove_ticker_stops_streaming: xfail — DELETE /api/watchlist/{ticker} calls source.remove_ticker (verified via mock)
  </behavior>
  <action>
Create `backend/tests/test_watchlist.py`. Do not import `app.routers.watchlist` (it doesn't exist yet).

Use `pytest.mark.xfail(reason="WATCH-XX: router not implemented", strict=True)` on every test.

For test_add_ticker_starts_streaming and test_remove_ticker_stops_streaming: the mock of `app.state.source` is not yet possible without the router, so these tests should simply call the endpoint and use `pytest.fail("not implemented")` after failing to import the router — same pattern as test_snapshot_background_task.

Each test uses `TestClient(app)` as context manager.

Follow project style conventions.
  </action>
  <verify>
    <automated>cd /Users/steve/projects/finally/backend && uv run --extra dev pytest tests/test_watchlist.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>pytest collects 5 tests from test_watchlist.py; all show XFAIL; no ERRORS in collection</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green (existing tests unaffected)</name>
  <files></files>
  <action>
Run the full test suite to confirm the 84 existing tests still pass and the 14 new stubs are all XFAIL. No regressions allowed.

If any existing test breaks, identify the cause (likely an import side-effect) and fix the stub files — do not modify existing test files.
  </action>
  <verify>
    <automated>cd /Users/steve/projects/finally/backend && uv run --extra dev pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All 84 prior tests pass; 14 new tests show XFAIL; zero ERRORS or unexpected failures</done>
</task>

</tasks>

<verification>
cd /Users/steve/projects/finally/backend && uv run --extra dev pytest tests/ -v --tb=short

Expected outcome:
- 84 previously passing tests: PASSED
- 9 portfolio stubs: XFAIL
- 5 watchlist stubs: XFAIL
- 0 ERRORS
- 0 unexpected failures
</verification>

<success_criteria>
- backend/tests/test_portfolio.py exists with 9 named test functions, each xfail
- backend/tests/test_watchlist.py exists with 5 named test functions, each xfail
- `uv run --extra dev pytest tests/ -q` exits 0 (xfail is not a failure)
- No import errors during collection
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-watchlist-api/02-01-SUMMARY.md`
</output>
